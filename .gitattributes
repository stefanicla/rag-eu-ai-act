# Normalise line endings; keep corpus handling sane.
* text=auto eol=lf

# Scripts must keep LF + exec bit on checkout.
*.sh text eol=lf

# Lockfiles and data: treat as binary-ish for diff purposes.
uv.lock -diff
*.pdf binary
*.bin binary
*.gguf binary
*.safetensors binary
